AlumnaAssociation.jsx

import React from "react";

export default function AlumnaAssociation() {
  return (
    <div style={styles.page}>
      {/* NAVBAR */}
      <div style={styles.navbar}>
        <div style={styles.navLeft}>
          <img src="/images/logo.png" alt="Logo" style={styles.logo} />
          <span style={styles.navTitle}>Alumni Connect</span>
        </div>

        <div style={styles.navLinks}>
          <span>HOME</span>
          <span>ANNOUNCEMENTS</span>
          <span>QUESTIONNAIRE</span>
          <span>ABOUT</span>
          <span>CONTACT US</span>
          <span>ALUMNI AFFAIRS</span>
        </div>

        <div style={styles.avatar}>AG</div>
      </div>

      {/* TITLE */}
      <div style={styles.titleSection}>
        <h1 style={styles.mainTitle}>ALUMNA ASSOCIATION</h1>
        <p style={styles.subTitle}>LIST OF OFFICERS</p>
      </div>

      {/* IMAGE SECTION */}
      <div style={styles.imageWrapper}>
        <img
          src="/images/elementary-bg.jpg"
          alt="Elementary Department"
          style={styles.image}
        />

        <div style={styles.overlay}></div>

        <div style={styles.imageText}>
          <h2 style={styles.imageTitle}>ELEMENTARY DEPARTMENT</h2>
          <p style={styles.imageSubtitle}>
            Located across the Wesley Divinity School Apartment.
          </p>
        </div>
      </div>

      {/* SCROLL CONTENT */}
      <div style={styles.scrollContent}>
        <h3>OFFICERS</h3>
        <p>
          President<br />
          Vice President<br />
          Secretary<br />
          Treasurer<br />
          Auditor<br />
          PRO
        </p>
      </div>
    </div>
  );
}

const styles = {
  page: {
    height: "100vh",
    overflowY: "scroll",
    fontFamily: "Arial, Helvetica, sans-serif"
  },

  navbar: {
    backgroundColor: "#6EC1FF",
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    padding: "10px 40px"
  },

  navLeft: {
    display: "flex",
    alignItems: "center",
    gap: "10px",
    color: "#fff"
  },

  logo: {
    height: "40px"
  },

  navTitle: {
    fontWeight: "bold",
    fontSize: "18px"
  },

  navLinks: {
    display: "flex",
    gap: "25px",
    color: "#fff",
    fontSize: "14px"
  },

  avatar: {
    width: "40px",
    height: "40px",
    borderRadius: "50%",
    backgroundColor: "#777",
    color: "#fff",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    fontWeight: "bold"
  },

  titleSection: {
    textAlign: "center",
    padding: "40px 0"
  },

  mainTitle: {
    color: "#0A3DB8",
    fontSize: "36px",
    letterSpacing: "2px",
    margin: 0
  },

  subTitle: {
    color: "#1E5ED6",
    fontSize: "18px",
    marginTop: "10px"
  },

  imageWrapper: {
    position: "relative",
    width: "100%",
    height: "420px"
  },

  image: {
    width: "100%",
    height: "100%",
    objectFit: "cover"
  },

  overlay: {
    position: "absolute",
    inset: 0,
    backgroundColor: "rgba(0,0,0,0.45)"
  },

  imageText: {
    position: "absolute",
    inset: 0,
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    color: "#fff",
    textAlign: "center"
  },

  imageTitle: {
    fontSize: "42px",
    letterSpacing: "3px",
    fontFamily: "Georgia, serif"
  },

  imageSubtitle: {
    fontSize: "18px",
    marginTop: "12px"
  },

  scrollContent: {
    padding: "80px",
    textAlign: "center"
  }
};


ROUTE

use Illuminate\Support\Facades\Route;

Route::get('/alumna-association', function () {
    return view('app');
});


.BLADE
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Alumna Association</title>
    @viteReactRefresh
    @vite('resources/js/app.jsx')
</head>
<body>
    <div id="app"></div>
</body>
</html>


